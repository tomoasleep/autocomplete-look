# autocomplete-look package

This package completes English words by using `look` command.  
This package requires [autocomplete+](https://github.com/atom/autocomplete-plus) package and `look` command.
